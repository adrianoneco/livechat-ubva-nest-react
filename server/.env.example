# Database
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=livechat
DATABASE_URL=postgres://postgres:postgres@localhost:5432/livechat

# JWT
JWT_SECRET=your-secret-key-change-this
JWT_REFRESH_SECRET=your-refresh-secret-change-this
JWT_EXPIRES_IN=1h
JWT_REFRESH_EXPIRES_IN=7d

# Server
PORT=3000
NODE_ENV=development
FRONTEND_URL=http://localhost:5173

# Global API Key (optional - for server-to-server auth)
GLOBAL_API_KEY=

# Groq AI (for transcription and AI features)
GROQ_API_KEY=

# Storage (local or s3)
STORAGE_TYPE=local
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1
AWS_BUCKET_NAME=
AWS_ENDPOINT=

# Evolution API Database (optional - for group info)
EVOLUTION_DATA_URL=
